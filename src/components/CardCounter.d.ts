import "../styles/CardsCounter.scss";
type CardCounterProps = {
    cardsCounter: number;
    totalCards: number;
};
declare function CardCounter({ cardsCounter, totalCards }: CardCounterProps): import("react/jsx-runtime").JSX.Element;
export default CardCounter;
